version: '2'
services:
  manytoone-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/manytoone/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=manytoone
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
